import 'package:flutter/material.dart';

class GymCard extends StatelessWidget {
  const GymCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Create car for the gym that can be shown in the home
    // This widget is common and can be reused in different pages
    return Container();
  }
}
